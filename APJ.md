or create a new repository on the command line

	echo "# addressbook-APJ" >> README.md
	git init
	git add README.md
	git commit -m "first commit"
	git remote add origin https://github.com/alwinpinakasj/addressbook-APJ.git
	git push -u origin master


push an existing repository from the command line

	git remote add origin https://github.com/alwinpinakasj/addressbook-APJ.git
	git push -u origin master

local is master
remote is origin